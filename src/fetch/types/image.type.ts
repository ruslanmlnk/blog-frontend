export interface Media {
  image: {
    url: string;
    width: number;
    height: number;
    alt: string;
  }
}

export interface AltMedia {
    url: string;
    width: number;
    height: number;
    alt: string;
}